# MultiMood
A repo to build and train and multimodal model for sentiment and emotion classification on movies and tv-shows


# Environment Setup

```bash
conda create python=3.10 -n multimood -y
conda activate multimood
pip install -r requirements.txt
```